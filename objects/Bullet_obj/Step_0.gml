/*
var inst=instance_nearest(x,y,Maiko_obj);
if(inst==noone){
	return;
}

var to_dis=point_distance(x,y,inst.x,inst.y);
if(to_dis>200){
	return;
}

var to_dir=point_direction(x,y,inst.x,inst.y);

var diff=angle_difference(direction,to_dir);
if(diff>0){
	direction-=1;
}
else{
	direction+=1;
}
*/